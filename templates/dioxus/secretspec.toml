[project]
name = "musicaloft-web"
revision = "1.0"

[profiles.default]
DATABASE_URL = { description = "PostgreSQL connection string" }
REDIS_URL = { description = "Redis connection URL for caching" }

[profiles.development]
DATABASE_URL = { default = "postgres://musicaloft:sillylittlepassword@localhost/musicaloft_dev" }
REDIS_URL = { default = "redis://localhost:6379" }

[profiles.production]
# Production profile inherits all secrets from default profile
# Only define secrets here that need different values or settings than default
